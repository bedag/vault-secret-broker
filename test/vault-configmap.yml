apiVersion: v1
kind: ConfigMap
metadata:
  name: (( concat "vault-" $VSB_DEV_ID || "dev" ))
  labels:
    dev-id: (( grab $VSB_DEV_ID || "dev" ))
data:
  vault-config.yml: (( file "vault-config.yml" ))